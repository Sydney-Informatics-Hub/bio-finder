#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# If no arguments provided, run in interactive mode
if [ $# -eq 0 ]; then
    exec "$SCRIPT_DIR/biofinder_client.py" interactive
else
    exec "$SCRIPT_DIR/biofinder_client.py" "$@"
fi
